mixin input({type = "text", name, pattern, placeholder, required, tabindex, mix}={})
  .input(class= mix)
      input.input__field(
        type = type
        name = name
        placeholder = placeholder
        pattern = pattern
        required = required
        tabindex = tabindex
      )&attributes(attributes)
      block