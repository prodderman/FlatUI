include ../search/search

mixin header-noauth({} = {})
  .header
    .header__logo
      a.header__logo-link(href="#")
        img(src=require('../../global/favicon.png')).header__logo-img
    .header__content
      a.header__auth-link(href="authorization.html" data-pjax=true) Sing in
       
    .header__search-wrap
      +search().header__search